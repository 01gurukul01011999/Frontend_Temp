export default function HelpFormJson() {
  // This file existed previously as an empty legacy file. Keep a tiny stub to satisfy
  // ESLint rules (filename-case and non-empty files). It's intentionally minimal.
  return null;
}
