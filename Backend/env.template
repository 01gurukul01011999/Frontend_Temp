# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Backend Server
PORT=4000

# JWT Configuration (if still needed for legacy)
JWT_SECRET=your_jwt_secret
